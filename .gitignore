__pycache__/
*.py[cod]
*.egg-info/
.venv/
/docs/superpowers/
venv/
.env
.tokensave
/docs

# stack.env and .env hold local config — only the .example files are tracked
stack.env
.env

# servers.json holds real credentials — only servers.json.example is tracked
config/servers.json
